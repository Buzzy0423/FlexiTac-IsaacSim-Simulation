# 已确认决策

确认日期：2026-09-15。当前实现进度见 [STATUS.md](STATUS.md)，本页只维护仍有效的配置约束。

## 手指与触觉表面

- 使用官方 Vega-1U `dexmate-urdf==0.8.4` 的 `dexs_gripper`，在本仓库维护定制副本。
- 用户 STL 是 TPU 指体，不是整个手指连杆；保留金属件、安装孔、关节原点、轴和 26 mm 宽度。
- 用户确认 FlexiTac 物理外形为 **64 × 24 mm**；仿真／打印的凸起平面均改为 **66 × 26 mm**，四周各留 1 mm。
  保持凸起近端与安装区域不动，向指尖延长约 5.236 mm，接触区下方指体同步沿长度方向延长。
- **仿真凸起比最初 STL 低 0.5 mm；打印凸起比同一最初 STL 低 0.8 mm。** 两个减量不是累计值。
- 仿真凸起已经代表传感器及防滑垫的最终外表面，不再叠加一层垫片几何。
- 用户确认对齐 ALOHA：在现有凸起平面沿外法线偏移 **2.1 mm** 放置虚拟触觉采样面；不增加实体碰撞层。
  每指 12×32 点、间距 2 mm，居中覆盖 22×62 mm 的采样中心范围。
  使用目标物体的有符号距离，输出 `clamp(5000 × max(-distance_m, 0), 0, 10) / 10`。
  采样面在实体外，因此触觉可在实体真正接触前出现；只查询明确注册的物体，不把机器人自身作为触觉目标。
- 仿真几何零位中心截面间隙约 0.009 mm，目标是近似轻触；这不等于 PhysX 接触已经验证。
- 当前打印面比仿真低 0.3 mm。如果每侧再贴 1 mm 垫片，实物最终会比仿真多 0.7 mm/侧。
  这个厚度差已告知用户；沿用其确认的减量，不擅自重做打印件，也不引入复杂校准流程。

## 桌子与工作区

- 使用用户提供的 **VentionAssembly_312098_v6.STEP** 官方模型。
- 名义桌面 1530 × 1080 mm、高 790 mm；完整 CAD 外包络约 1564 × 1114 × 791 mm，保留真实比例。
- 机器人放在 **1080 mm 短边中央，面向桌内**。
- 世界原点是近端短边中心在地面的投影；+X 进入桌内，+Y 向机器人左侧，+Z 向上。
- 近机器人一端铺设 **60 × 100 cm、厚 1 cm** 的桌板：按 60 cm 沿 +X、100 cm 沿 Y 实现；
  近沿与桌面 X=0 对齐，左右居中，各留 4 cm。板底贴桌面，板顶约 0.801 m；颜色暂用棕色。
- 用户要求移动底座紧贴桌子。基座位置为 `[-0.3889112563, 0, 0.187] m`，
  使变换后的底座前缘外包络与桌架近端平面 X=0 相切，替代原约 10 cm 间距；这属于模型摆放约束。
- 桌子采用静态三角面碰撞保留槽与支架；统一灰色仅为暂用显示材质。

## 环境与数据路线

- 独立 Conda 环境 `dexmate_isaacsim`，对应本仓库的 Isaac Sim 5.1.0 路线。
- 首批观测是 **vision + tactile**，机器人状态和动作作为条件；force／torque 在量的定义明确后再接入。
- 固定头部左右双目和左右腕相机共四路，腕相机跟随手臂；首轮不随机化安装布局。
- 2026-09-16：用户提供自装腕相机外壳和 C960 支架 STL，用于评估替换官方安装并查看效果。
  保留输入几何；安装可行性预览不代表已激活正式资产，也不将 STL 姿态视为标定光学外参。
- 2026-09-16：用户授权读取本机两台腕相机并配置参数，指出已存在 udev 规则。
  沿用 `/dev/dexmate-wrist-left`（A260401000123002）和 `/dev/dexmate-wrist-right`（A260421000122337），不重建左右映射。
- 2026-09-16：用户确认当前仿真无需精确实机标定，先用官方 C960 的 90° 对角视场估算内参，
  采用居中、无畸变针孔模型；按自装 STL 的镜头前端中心／法线设置名义安装，再验证视野和抓取。
  腕相机采用 640×360，与实机 1280×720 保持 16:9；头部两路仍为 640×480。名义值不写作实测标定。
- 2026-09-16：用户查看与实机录像的对照后，确认当前阶段暂不进行精确相机标定，继续沿用名义参数。
  所提供的历史录像实际为 960×600（16:10），不同于本次实机探测的 1280×720（16:9）；后续模型输入仍需明确裁剪／缩放。
- 优先完成小规模接触／释放验证，再做同步序列记录与规模扩展。
- 用户确认先实现单手自由物体的夹持、抬升约 5 cm、保持 2 秒、放回和松开；通过后再扩展双手及同步记录。
- 用户确认接入可重复 reset/step；以轮次、物理步号和仿真时间对齐。
- 2026-09-16：用户确认 demo 只需 30 Hz，后续 RGB／触觉／状态／动作统一以 30 Hz 保存（recorder v2）。
  物理与控制、内部夹持验收暂保留 120 Hz；每四步抽样，不改变接触积分。旧 v1 记录保持原样。
- 2026-09-16 性能优化：每四个物理步执行一次渲染，启用同步渲染完成设置；采集不创建闲置的摆放视角。
  必须同时核对物理时钟、原始渲染帧前进与图像内容，不能只靠重映射后的时间戳判断同步。
- 用户要求后续组合预览统一为 **RGB 在上、同一时刻的四片 tactile 热图在下**；每片维持 12×32，按视频帧索引取对应触觉。
- 2026-09-16：离线读取接口统一由 `dex_wire/data` 维护，沿用四相机名称与 16 维双臂／夹爪排列。
  仿真兼容旧 30/120 Hz 与新 30/30 Hz，保留完整关节排列及动作后观测语义；实机缺失触觉不补零。
- 2026-09-16：用户同意先做左手当前方块的小规模位置／朝向变化采集，逐轮检查抓取、碰撞、同步，
  保存失败结果，并用上方四相机 RGB、下方同帧触觉检查数据。
- 原 FlexiTac 输出根据距离、刚度及限幅估算触觉标量，不直接读取 PhysX 接触力。
- 自碰撞关闭仅是诊断条件，不能作为夹持或两指接触已通过的依据。
