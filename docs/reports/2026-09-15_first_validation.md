# 2026-09-15 首轮仿真验证

这是当日验证证据，后续状态见 [STATUS](../STATUS.md)。
精简原始数值随仓库保存于 [validation_summary.json](2026-09-15_validation_summary.json)。

## 验证结论

| 用例 | 条件 | 结果 |
| --- | --- | --- |
| 基础运行 v3 | CPU PhysX、RTX、0.1 m 落体方块 | 通过；中心高度 0.0499999 m，640×480 RGBA 有效 |
| 自碰撞 v8 | 官方桌子，开启机器人自碰撞 | 未通过；内部接触导致关节明显偏离目标 |
| 场景对照 v11 | 官方桌子，关闭机器人自碰撞 | 自由开合、桌面接触及场景渲染通过；进程退出码 0 |
| 四路相机 v11 | 名义 URDF 挂载、暂用内参 | 均输出 640×480 图像，已检查头部及腕部视图 |

v11 夹爪指令为 `0.4 → 0 → 0.4 rad`，整机关节最大目标偏差约 0.0106 rad。
50 mm 方块静止中心高度为 0.81600016 m，对应 CAD 桌面高度 0.79100000 m。
Dexmate 导入 131 个非空网格；桌子包含 144 个组件、616112 个三角面。

**通过的范围不包含两指间接触、抓取、FlexiTac 触觉或实机相机标定。**
关闭机器人自碰撞会同时关闭两指之间的 PhysX 碰撞；机器人与外部物体／桌子的碰撞保留。

## 自碰撞失败与已处理问题

失败运行记录了底座／躯干、头部／躯干、腕部及两指等接触对；
它们说明需要检查碰撞近似与内部连接排除关系，不能仅凭对照通过排除问题。
事件计数不是接触力，也不能单独确定哪一对是根因。

已处理：GLB 节点变换烘焙后外观恢复；USD 根变换精度对齐；截图不再暂停后续物理步进；
腕相机近裁剪面避开自身外壳；STEP 转换补齐开放壳体，避免漏脚垫造成错误高度。

## 环境与来源

- Isaac Sim 5.1.0.0，Python 3.11，专用环境 `dexmate_isaacsim`；未安装整套 Isaac Lab。
- RTX 4080 SUPER，驱动 535.309.01；使用 NVIDIA 已记录的 R535 Vulkan 误报处理方式。
- 运行器预加载 Conda C++ 库；完整逐扩展退出曾挂起，最终使用设置退出码后的默认快速关闭。
- 输入定制 URDF SHA256：`a6a931cddd341bb6ec3bc724e91cdaeac86d67e6964d66de70e2344ca3d3c394`。
- 官方 STEP SHA256：`bafc462548aa41a829252cef38ed14d03f457491d9437ef4da5f9749fa8d8f06`。
- 参数与输出哈希另见两个资产目录的 manifest。`pip check`、Ruff 与 shell 语法检查通过。

## 全量证据索引（本机，不提交）

相对 `Isaacsim_tactile_env/output/`：

- `dexmate_runtime_20260915_v3/`：`runtime.json`、图像和启动日志。
- `dexmate_scene_20260915_v8/`：自碰撞失败 JSON 与启动日志。
- `dexmate_scene_20260915_v11/`：成功对照 JSON、场景／四相机图像、USD、日志和依赖快照。

新克隆不包含这些运行输出；按 [运行指南](../guides/runtime.md) 在新目录重建。
保留成功与失败证据，不将这份报告更新成后续日期的结果。
