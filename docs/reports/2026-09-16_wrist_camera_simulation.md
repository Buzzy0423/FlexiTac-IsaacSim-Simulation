# 自装 C960 腕相机：名义光学接入与抓取回归

用户确认当前仿真先使用估算内参，无需等待精确实机标定。左右自装相机已接入默认 URDF／workspace；
**候选连续两轮、激活后默认入口一轮，合计 3/3 完整抓取通过**。本次没有重新执行十轮姿态变化。
详细数值及来源：[摘要 JSON](2026-09-16_wrist_camera_simulation_summary.json)。

## 当前模型

- 资产版本 `meshes/wrist_c960_v1/`：移除官方 camera visual 及底座中两块旧安装件，接入两份用户 STL。
  原始 STL 与 `upstream/` 保持原样；保存修改前 URDF／manifest，更新当前 manifest 哈希。
- 旧底座碰撞包含原相机支架，不能保留。改为六个保留主要 CAD 实体的独立凸包；
  新支架和相机使用其表面网格并沿用 PhysX 凸分解。凸包会填平一些凹槽，是明确的碰撞近似。
- `L/R_camera_link` 改为镜头前端圆形表面中心，夹爪局部位置约
  `[0.201711, -77.864660, -5.515453] mm`，roll 约 **−24°**。
  光学坐标 +X 向右、+Y 向下、+Z 沿镜头前表面法线；运行时转换为 USD 相机 −Z 前、+Y 上。
  光心取镜头前表面中心是名义假设，并非镜头内部真实投影中心测量。
- 相机质量沿用官方旧相机的暂用 25.3335 g，新惯量按 STL 均匀密度、该暂用质量求得；
  底座／支架合并质量惯量仍继承旧值。未宣称为实机 C960 总成质量惯量。

## 名义内参

[EMEET 官方](https://emeet.com/products/webcam-c960) 给出普通 C960 的 90° **对角**视场与 2.88 mm 焦距。
假设保留完整视场、正方形像素、居中主点、针孔投影；零畸变仅为仿真简化。

| 配置 | 当前值 |
| --- | --- |
| 腕相机输出 | 640×360、30 Hz；相当于实机 1280×720 同比例减半 |
| fx / fy | 367.151195 px |
| cx / cy | 320 / 180 px |
| 名义水平／垂直视场 | 82.149° / 52.234° |
| USD focal length | 2.88 mm |
| USD 水平／垂直 aperture | 5.020275 / 2.823905 mm，按目标视场反推 |
| 近／远裁剪 | 0.001 / 20 m |

头部两路仍用原 640×480 名义参数。每轮记录 `data_contract.camera_models`，包含 K、图像尺寸、来源及名义状态。
四相机／触觉组合图保留 16:9 腕图比例，格子内留边，不进行非等比拉伸。
实机 profile 的未知标定值仍为空，没有被仿真近似参数覆盖。

## 验证结果

CPU PhysX、自碰撞开启，沿用固定簇与相邻关节过滤；未关闭自碰撞来规避新几何问题。

| 项目 | 三轮结果 |
| --- | --- |
| 夹持→抬升→保持→放回→释放 | 3/3 通过 |
| 保持期间抬升 | 50.088–50.117 mm |
| 最大相对滑移 | 0.0514 mm |
| 双指承载／双片触觉、释放稳定性 | 全部通过 |
| 检测到的非预期机器人接触 | 无 |
| 记录长度 | 每轮 540 条状态／触觉，五台相机各 540 帧 |
| RGB 与仿真时钟最大误差 | 0 s；原始渲染帧前进检查通过 |
| 连续两轮 reset | 状态／速度／物体位姿一致性通过 |
| 共用 Dex Wire 离线读取 | 3/3 通过，正确读取混合图像尺寸与时间窗口 |

候选两轮中，夹持／抬升／保持／放回各合计 225 个保存帧的目标中心全部落在左腕相机视锥内，
投影约 `[319.55–319.62, 218.39–218.56] px`。这是视锥检查，不是遮挡检测；
另已目视确认抓取保持帧中物体可见。右腕初始视野和成像正常，右手未参与抓取任务。
候选与激活后完整 30 Hz 关节位置、物体位置、触觉和动作数组逐项完全一致。

## 输出与检查

- 候选：`output/wrist_camera_validation_20260916_v2/`，两轮。
- 默认入口复测：`output/wrist_camera_active_20260916_v1/`，一轮。
- 两者首轮均含 `four_cameras_preview.mp4/png`，上方四相机，下方同帧触觉。
- `tools/build_dexmate_wrist_camera.py` 为派生／激活工具；`tools/dexmate_camera_model.py` 为名义针孔参数计算。
- 27 项离线检查通过；新增检查覆盖对角／水平视场区分、缩放、头部参数不变和非法输入；修改脚本 Ruff 通过。
  离线测试用 Isaac Sim 安装内的 Warp 扩展路径，触觉核在 CPU 执行；实际 GPU 场景另外以上述三轮验证。
- 正式复测后修正了 URDF 的旧说明注释；去掉 XML 注释后的节点／属性与保存的
  `source_urdf_snapshot.xml` 完全一致，该快照哈希对应场景报告。源／派生哈希、链接和忽略规则均检查。

首次候选 v1 因缺少触觉采样需要的同目录 manifest，在启动期间失败，未完成抓取；
补齐候选 manifest 及手指来源路径后 v2 通过。失败目录保留，不计入通过次数。

本次通过范围仅为现有左手基线轨迹与当前物体，不代表全关节避碰、右手任务、全部物体，
也不代表实机与仿真像素级配准。下一步用新相机配置复测已有十轮姿态变化，先保持名义相机参数。

## 同日补充：实机录像对照与输出整理

用户提供的录像为相邻 `robot-policy-deploy` 项目下
`artifacts/2026/09/04/sessions/session-20260904T213600Z-925b1958c6e2/runs/run-20260904T215053Z-71018dfb37a8/cameras/dexmate.wrist.right_rgb.mp4`。
该录像为 960×600、20 fps；当前仿真右腕为 640×360、30 fps。抽帧对照保存在
`output/wrist_camera_real_comparison_20260916_v1/comparison.jpg`，不同开合姿态的外观对照不作为内外参标定。
用户确认暂不进行精确标定；模型输入的裁剪／缩放仍需统一。

本地输出已按 [清理记录](2026-09-16_output_cleanup.json) 移除可重建的导入网格和 USD，
本报告引用的视频、数值、验收报告、候选资产和源 URDF 快照均保留，原始实机录像未修改。
