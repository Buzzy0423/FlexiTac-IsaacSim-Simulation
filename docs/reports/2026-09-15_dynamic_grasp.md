# 左手自由物体抓取验证

日期：2026-09-15（太平洋时间）。本机运行 `dexmate_grasp_20260915_v1`，退出码 0，整体报告 `passed`。

## 条件

保持官方 Vention 桌、近端桌板及当前 `pad_66x26_v3` 四指模型，机器人自碰撞开启。
新增 40×30×80 mm、50 g 动态长方块，位于桌板 XY `[0.34935, 0.22] m`。
左手侧向抓取，动作由 URDF IK 和位置驱动执行；物体无附加约束，不覆盖运行时位姿，不关闭重力。
左手／方块静摩擦 0.8、动摩擦 0.6，恢复系数 0；均为暂用仿真参数。

首轮可达性计算显示，在计划位置使用水平侧向夹持更合适，采用当前夹爪零位朝向。
动作包括预定位、下降、闭合、抬升、保持、放回、松开、退出；没有将 IK 当作通用避障规划器。
参数和复现命令见[抓取指南](../guides/grasp.md)。

## 实测结果

| 项目 | 结果 |
| --- | --- |
| 物体初始中心高度 | 0.841000 m |
| 保持阶段抬升高度 | 49.9528～49.9808 mm |
| 保持时长 | 240 步，2 秒 |
| 相对闭合末尾的最大夹爪坐标位置变化 | 0.0902 mm |
| 左手两指持续受力帧占比 | 100%、100% |
| 双片触觉保持／释放检查 | 通过；释放末尾均为零 |
| 放回后的物体中心高度 | 0.841000 m |
| 最终 XY 偏移 | 约 0.626 mm |
| 意外机器人自碰撞／撞桌 | 未检测到超过 1e-5 N·s 的冲量 |
| 桌板方块接触、自由开合、四路相机 | 通过 |

任务序列 2160 帧，仿真时间约 6.0333～24.0250 s；包含前置自由开合的完整触觉序列共 2880 帧。
同阶段相邻触觉记录的物理步号差均为 1，采样 120 Hz。
9 项单元检查及相关 Python Ruff 检查通过；新增检查确保“物体不升高”“仅有虚拟触觉但无持续物理接触”
和“明显滑移／机器人撞桌”不能被误判为成功。

证据：`Isaacsim_tactile_env/output/dexmate_grasp_20260915_v1/` 中的 `grasp_report.json`、
`grasp_sequence.npz`、`grasp_replay.gif`、各阶段 PNG 和整体 `validation.json`。
可提交摘要：[2026-09-15_dynamic_grasp_summary.json](2026-09-15_dynamic_grasp_summary.json)。

## 输出整理

归档了此前 33 次历史运行的报告、图像、序列及预览，删除可重建的 USD／USDA／OBJ／运行 URDF，释放 **4.596 GiB**。
归档位置 `output/archive/<原运行名>/`。源资产未改动；当前触觉基线 v3、几何预览及热图预览保留。
加入本次完整抓取输出后，`output` 约 760 MiB。
清单见[清理记录](2026-09-15_output_cleanup.json)。

## 范围

只验证这一物体、左手姿态和轨迹；不代表任意物体抓取、实物动力学标定或所有姿态无碰撞。
图像回放采用独立观察相机；机器人四相机尚无全程同步记录。
下一步为 reset/step 生命周期和多模态时间对齐，再扩展其他物体与双手任务。
